export interface enroll {
  user_id: number;
  course_id: string;
  sem: number;
  year: number;
}

export interface multipleEnroll {
  rollNo: string;
  course_id: string;
  year: number;
}
