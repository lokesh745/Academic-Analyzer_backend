export interface result {
  sem: number;
  courseId: string;
  departmentId: string;
  rollNo: string;
  credits_earned: number;
}

export interface result_multiple {
  course_id: string;
  rollNo: number;
  mse: number;
  ese: number;
  isa: number;
  year: number;
}
